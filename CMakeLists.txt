cmake_minimum_required(VERSION 2.6)
project(metro-spb C)
include_directories(/home/lolka/.pebble-sdk/SDKs/current/sdk-core/pebble/basalt/include)
file(GLOB SOURCE_FILES src/c/*.c src/c/locales/*.c)
add_executable(metro-spb ${SOURCE_FILES})
add_custom_target(pebble-build COMMAND)
